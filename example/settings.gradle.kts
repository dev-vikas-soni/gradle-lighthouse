pluginManagement {
    repositories {
        mavenLocal()  // For local testing before publishing
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "example"
